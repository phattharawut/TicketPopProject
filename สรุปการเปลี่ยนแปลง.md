## 📝 สรุปการเปลี่ยนแปลง — คนที่ 7 (My Tickets + QR + History)

---

### ✅ ไฟล์ที่แก้ไข

**1. `TicketViewModel.kt`**
- เปลี่ยนจาก Mock Data → เรียก API จริง
- เปลี่ยน `ViewModel()` เป็น `AndroidViewModel(application)` เพื่ออ่าน SharedPrefs
- `loadMyTickets(userId)` เรียก `GET /api/users/{userId}/tickets`
- `loadTicketDetail(ticketId)` เรียก `GET /api/tickets/{ticketId}`
- อ่าน JWT token จาก `SharedPrefs` ด้วย key `Constants.KEY_JWT_TOKEN`

**2. `MainActivity.kt`**
- เพิ่ม route 3 เส้นทางใหม่:
  - `ROUTE_MY_TICKETS` → `MyTicketsScreen`
  - `ROUTE_TICKET_QR` → `TicketQrScreen`
  - `ROUTE_TICKET_HISTORY` → `TicketHistoryScreen`
- ตอนนี้ `userId` hardcode เป็น `2` ชั่วคราว รอคนที่ 2

**3. `app/build.gradle.kts`**
- เปลี่ยน `compileSdk` และ `targetSdk` จาก `35` → `36`

**4. `AndroidManifest.xml`**
- เพิ่ม `<uses-permission android:name="android.permission.INTERNET" />`
- เพิ่ม `android:networkSecurityConfig="@xml/network_security_config"`

**5. `res/xml/network_security_config.xml`** (ไฟล์ใหม่)
- อนุญาต HTTP กับ `10.0.2.2` (XAMPP บน Emulator)

**6. `data/remote/ApiService.kt`**
- เพิ่ม 2 endpoint:
```kotlin
@GET("api/users/{userId}/tickets")
suspend fun getMyTickets(
    @Path("userId") userId: Int,
    @Header("Authorization") authorization: String
): ApiResponse<List<Ticket>>

@GET("api/tickets/{ticketId}")
suspend fun getTicketDetail(
    @Path("ticketId") ticketId: Int,
    @Header("Authorization") authorization: String
): ApiResponse<Ticket>
```

---

### ⚠️ สิ่งที่คนอื่นต้องทำต่อ

**คนที่ 2 (Auth)**
หลัง login สำเร็จต้อง save ลง SharedPrefs ด้วย:
```kotlin
prefs.edit()
    .putString(Constants.KEY_JWT_TOKEN, userData.token)
    .putInt(Constants.KEY_USER_ID, userData.userId)
    .apply()
```

**คนที่ 1 (NavHost)**
แก้ `MainActivity.kt` เปลี่ยน `userId` จาก hardcode เป็นอ่านจาก SharedPrefs:
```kotlin
val userId = prefs.getInt(Constants.KEY_USER_ID, 0)

composable(Constants.ROUTE_MY_TICKETS) {
    MyTicketsScreen(navController = navController, userId = userId)
}
composable(Constants.ROUTE_TICKET_HISTORY) {
    TicketHistoryScreen(navController = navController, userId = userId)
}
```

---

### 🗄️ Backend — สิ่งที่แก้ใน `server.js`

เปลี่ยน `c.show_date AS showDate` เป็น `DATE_FORMAT(c.show_date, '%Y-%m-%d') AS showDate` ใน 2 endpoint:
- `GET /api/users/:userId/tickets`
- `GET /api/tickets/:ticketId`

เพื่อให้ format ตรงกับที่ ViewModel ใช้เปรียบเทียบวันที่ครับ